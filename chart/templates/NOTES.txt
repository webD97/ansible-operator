ansible-operator has been installed into namespace "{{ .Release.Namespace }}".

This namespace is now load-bearing, not just a deployment target: the operator uses it to store
its own SSH certificate authority Secret, and creates per-run Leases and ephemeral managed-ssh
proxy pods/Secrets/NetworkPolicies here at runtime. Do not point PlaybookPlans/ClusterInventories/
StaticInventories at this same namespace — those belong in your own tenant namespaces.

Next steps:
  1. Create a ClusterInventory or StaticInventory in the namespace where you want to run
     playbooks from.
  2. Create a PlaybookPlan referencing it.
  3. Watch progress: kubectl get playbookplan -n <your-namespace> -w
